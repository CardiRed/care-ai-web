const fs=require('node:fs');const path=require('node:path');const crypto=require('node:crypto');const out=path.join('dist','care-ai-web-v2.0.0');fs.mkdirSync(out,{recursive:true});for(const file of ['index.html','style.css','script.js','README.md','CHANGELOG.md'])fs.copyFileSync(file,path.join(out,file));fs.writeFileSync(path.join(out,'SHA256SUMS.txt'),['index.html','style.css','script.js','README.md','CHANGELOG.md'].map(f=>crypto.createHash('sha256').update(fs.readFileSync(path.join(out,f))).digest('hex')+'  '+f).join('\n')+'\n');console.log('Runnable folder: '+out);
